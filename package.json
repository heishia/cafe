{
  "name": "naver-oauth-mini-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Naver OAuth callback server for Railway deployment.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
